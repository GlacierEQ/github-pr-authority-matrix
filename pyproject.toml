[project]
name = "github-pr-authority-matrix"
version = "0.0.1"
description = "Scaffold: PR Authority Matrix (independent GitHub lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
