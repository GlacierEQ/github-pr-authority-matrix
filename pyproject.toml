[project]
name = "github-pr-authority-matrix"
version = "1.0.0"
description = "Signed pull-request authority, revocation, and side-effect dispatch engine"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
